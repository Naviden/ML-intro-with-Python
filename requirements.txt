numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3          # ConfusionMatrixDisplay.from_predictions, mean_absolute_percentage_error
xgboost>=2.0               # use_label_encoder removed (no longer needed)
joblib>=1.3                # model serialization
sentence-transformers>=2.5 # Case Study: text embeddings (pip install -U sentence-transformers)
